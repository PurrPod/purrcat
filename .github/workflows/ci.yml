name: PurrCat CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  backend-tests:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash -el {0}

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup Conda Environment
      uses: conda-incubator/setup-miniconda@v3
      with:
        activate-environment: CatInCup
        environment-file: environment.yml
        python-version: '3.10'
        auto-activate-base: false
        cache-environment: true

    - name: Install Additional Linting Tools
      run: |
        pip install ruff pyflakes

    - name: Python Syntax Check
      run: |
        python -m py_compile main.py
        echo "main.py syntax check passed"
        python -m compileall -q -f src/ || true
        echo "src/ directory syntax check completed"

    - name: Ruff Lint Check
      run: |
        ruff check src/ --ignore E501 --output-format concise || echo "Some lint warnings can be ignored"
        echo "Code style check completed"

    - name: Module Import Test
      env:
        PYTHONIOENCODING: utf-8
      run: |
        python test/test_imports.py

