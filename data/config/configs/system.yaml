# ============================================
# 系统基本配置
# ============================================

# Agent 使用的默认模型
agent_model: "openai:deepseek-chat"

# Embedding 模型
embedding_model: "BAAI/bge-small-zh-v1.5"
