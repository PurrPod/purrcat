### 用户偏好与工作风格
用户是程序员老板，话少直接，喜欢干货，不喜欢客套话和官方话，回复要精简
注重标点符号的全中文格式（全角符号）
论文格式要求细致严格

### 工作经验
- 每次用户发消息，必须先走 Memo search 查询相关记忆，不能跳过
- 主动调用记忆比被动注入更灵活高效，但前提是 agent 得养成调用习惯
- 使用docx-js生成Word文档：四号字=14pt=28half-points，首行缩进2字符=560twips，段前段后0.5行=120，1.2倍行距=288/auto，悬挂缩进用left+hanging
- 中文标点必须使用全角符号（全中文格式）