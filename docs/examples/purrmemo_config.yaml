# PurrMemo 记忆系统配置
# 启用后将 update_memo 的记忆推送到 PurrMemo 服务，而非仅写入本地 memory.md
purrmemo:
  # 是否启用 PurrMemo 集成
  enabled: false

  # PurrMemo 服务器地址
  host: "http://127.0.0.1:8000"

  # API 密钥（如果 PurrMemo 需要认证）
  api_key: ""

  # 连接超时（秒）
  timeout: 5
